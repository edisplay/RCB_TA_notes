### 2. Partners do 30 minutes
```
var args = process.argv.slice(2)
var returnArray = [];

var addAllBut = function (c){
	var total = 0;

	for(var b = 0; b < args.length; b++){

		if (b != c){
			total += parseFloat(args[b]);
		}
	}

	return total;
}

for(var i = 0; i < args.length; i++){
	returnArray.push(addAllBut(i));
}

console.log(returnArray);
```


### 3. Student do (5 minutes)
run this:

```
npm install request
```

inside of exercise1.js
```
var request = require('request');
request('http://www.omdbapi.com/?t=the+note+book&y=&plot=short&r=json', function (error, response, body) {
  if (!error && response.statusCode == 200) {
    console.log(JSON.parse(body)["imdbRating"])
  }
});
```


### 6. Partners do 30 minutes

```
var request = require('request');
var zip = process.argv[2];

var apiToHit = "http://api.openweathermap.org/data/2.5/weather?zip=" + zip + ",us&appid=d48e7a773995bce1fb6ca3289862dd64";

function consoleWeatherOut(temp){
	console.log("The temperature is");
	console.log(temp + ".");
}

function consoleMovieOut(bd){
	console.log(JSON.parse(bd)["Title"]);
	console.log("has an imdbRating of");
	console.log(JSON.parse(bd)["imdbRating"]);
	console.log("you should watch it.");
}

request(apiToHit, function (error, response, body) {
  if (!error && response.statusCode == 200) {
    var tempKelvin = JSON.parse(body).main.temp;
    var tempFar = Math.floor((tempKelvin - 273.15)* 1.8000 + 32.00);

	if (tempFar < 45) {
		request('http://www.omdbapi.com/?t=titanic&y=&plot=short&r=json', function (error, response, body) {
		  if (!error && response.statusCode == 200) {
		  	consoleMovieOut(body);
		  	consoleWeatherOut(tempFar);
		  }
		});
	}else if(45 <= tempFar <= 60){
		request('http://www.omdbapi.com/?t=titanic&y=&plot=short&r=json', function (error, response, body) {
		  if (!error && response.statusCode == 200) {
		    consoleMovieOut(body);
		    consoleWeatherOut(tempFar);
		  }
		});
	}else if(tempFar > 60){
		request('http://www.omdbapi.com/?t=the+terminator&y=&plot=short&r=json', function (error, response, body) {
		  if (!error && response.statusCode == 200) {
		    consoleMovieOut(body);
		    consoleWeatherOut(tempFar);
		  }
		});
	}
  }
});
```

# Copyright
Rutgers Coding Boot Camp (C) 2015. All Rights Reserved.